Note1: the folders named EX1, EX2, .. are in consecutive order with respect to the problems given.
(e.g. EX1 corresponds to chapter 2 exercise 10)
 
EX1:
This was pretty cool. I had to do some research to find the right (and most elegant) algorithm to implement so that the cubic solver isn't too long. Newton method is pretty primitive but it gets the job done here. Obtaining the null space without doing gaussian elimination was convenient. To be honest, I don't know the full extend of HouseholderQR. Multiplied by the identity matrix to type-cast.

EX2:
Figured out that << is overloaded so that instead of bit shift I can use it to fill row by row.

EX3:
setprecision() is very useful for making doubles readable. 

EX4:
This one took awhile, and lots of research had to be done. It turned out good and it looks like it works.

EX5:
For plotting I mostly just used the code from ACP-Examples/CH3/FUNCTIONVIS1 and FUNCTIONVIS2. This was stuff I learned last spring it was cool to program it. The plotting doesn't exactly work and I am not sure why. I can't see it too well on Mac, but it looks like there are points all around the edges.